## 2026-09-07T00:52:00Z — Revert PR #23 gitleaks reporting change (Claude Code)

- **Agent/tool:** Claude Code
- **Repos:** dizhaky/.github
- **Done:** Reverted the squash-merge of PR #23 (`1726bda`, "ci: report gitleaks finding locations in reusable secret scan") at the owner's request. Restores `.github/workflows/reusable-secret-scan.yml` to its pre-#23 state and removes `docs/system-log/2026-09-06.md`, which #23 had added. The tree after the revert is byte-identical to `1fa0d3e` (the base #23 merged onto) apart from this log entry. Separately, PR #24 was converted back to draft; PR #23 could not be converted because it was already merged.
- **Validation:** `python3 -m unittest discover -s tests` passed (3 tests). `.github/workflows/reusable-secret-scan.yml` parses as valid YAML. `git diff 1fa0d3e <revert>` is empty for tracked workflow/source files.
- **Commits/PRs:** revert of `1726bda`; branch `revert/pr-23`.
- **Follow-up:** PR #24 (`ci/nightly-scan-report-location`) is draft and currently conflicted against `main`; it references the deleted `docs/system-log/2026-09-06.md` path. If the gitleaks reporting work is wanted later, re-land #23 as a fresh PR rather than reopening the merged one.
