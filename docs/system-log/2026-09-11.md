
## 2026-09-11 — Fleet GraphQL quota mitigations for org auto-merge templates (DAN-3334) (Cursor Cloud)

- **Agent/tool:** Cursor Cloud Agent
- **Repos:** dizhaky/.github
- **Why:** Dotfiles #1205 already cut GraphQL from repo-local auto-merge; the org templates in this repo still used `gh pr view/list/merge` (GraphQL) on `*/15`, and `global_auto_merge.py` still did a GraphQL PR lookup on every open PR every 15 minutes across every admin repo — all as dizhaky (213320850) via `AUTO_MERGE_PAT`.
- **Done:**
  - `auto-merge.yml`: REST reads + REST merge; `*/30` + jitter + eligible-only list; no rate-limit retries; PAT identity documented
  - `global_auto_merge.py`: REST-normalized PR lookup; draft skip at list time; abort when GraphQL/REST remaining &lt; 100; enrollment (`gh pr merge --auto`) still GraphQL (no REST equivalent)
  - `global-auto-merge.yml`: cron `7,37 * * * *` (was every 15m)
- **Verified:** `pytest tests/test_global_auto_merge.py` — 52 passed; `bash -n` on auto-merge script
- **Follow-up:** Consumer repos that vendored a pre-DAN-3334 `auto-merge.yml` still need a template sync/rollout

## 2026-09-11T14:42:57Z — Roll out Secret Scan + failure alert to Agent-Reach and agentmemory (Cursor Cloud Agent)

- **Agent/tool:** Cursor Cloud Agent
- **Repos:** dizhaky/Agent-Reach, dizhaky/agentmemory (consumers); dizhaky/.github (templates)
- **Done:** Shipped central templates into the two repos this environment can write: `secret-scan.yml` (SHA-pinned reusable gitleaks) and `secret-scan-alert.yml` (`workflow_run` caller for default-branch red-scan issues; Slack fail-soft). `obsidian-vault` / `mcp-servers` remain out of token scope.
- **Validation:** Both PRs green including `scan / gitleaks`; squash-merged.
- **Commits/PRs:** Agent-Reach #4 (`02efbed`); agentmemory #19 (`061ea6a`).
- **Follow-up:** Optional `SLACK_WEBHOOK_URL` per repo; wire remaining consumers when access exists; post-merge `workflow_dispatch` rehearsal of alert paths.
