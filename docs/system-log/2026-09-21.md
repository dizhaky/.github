# 2026-09-21

## 2026-09-21T04:23:00Z — Harden post-merge review capture edge cases (Antigravity)

- **Agent/tool:** Antigravity
- **Repos:** .github
- **Done:** Address Codex review on PR #67: bound issue lookups with a 10-candidate cap (`head -n 10`), query native `closingIssuesReferences`, anchor closing keywords at word boundaries (`\b(?:close[sd]?|fix(?:e[sd])?|resolve[sd]?)`), require issue/review number in follow-up title match, scope keywords to `$REPO`, fail closed on issue fetch errors, anchor marker check, and recognize `copilot` in follow-up regex.
- **Commits/PRs:** this change
- **Follow-up:** none

## 2026-09-21T04:39:00Z — Harden post-merge capture against cap overflow, legacy titles, and repo dots (Antigravity)

- **Agent/tool:** Antigravity
- **Repos:** .github
- **Done:** Hardened `post-merge-review-capture.yml` against remaining edge cases: escape dots in repository name when regex-matching native `closingIssuesReferences` URLs so cross-repo matches like `dizhaky/xgithub` don't collide with `dizhaky/.github`, fail closed when candidate closing issues exceed 10-item cap, recognize legacy capture issue titles without review IDs (`Post-merge review on #...`), allow target numbers before follow-up phrase or numberless named bot follow-ups, handle optional `gh pr view closingIssuesReferences` failure gracefully, and filter out nulls before `unique[]` in jq scan.
- **Evidence:** Ran `pytest tests/test_post_merge_review_capture.py` (35 passed).
- **Closes:** #70
