## 2026-09-06T00:13:20Z — Retire Hermes review forwarding (Codex)

- **Agent/tool:** Codex
- **Repos:** .github
- **Done:** Replaced retired Hermes webhook forwarding with GitHub-native post-merge review capture. Added offline regressions, per-review event identity, and fail-closed authoritative issue lookup. Hosted automatic-review configuration remains separate.
- **Baseline:** Existing forwarding used wrong actor names, wrong HMAC input, unverified TLS, and retired endpoint; live endpoint returned 502. No runtime was restarted.
- **Verification:** 9 capture tests plus 3 existing tests, actionlint, and diff checks. The issue-lookup-failure test failed before the fail-closed fix.
- **Follow-up:** Authenticated hosted Code Review settings and Linear reauthentication remain required for those checks.

## 2026-09-06T00:58:51Z — Correct the shared isolation blueprint (Codex)

- **Agent/tool:** Codex
- **Repos:** .github.
- **Done:** Reproduced import-time credential leakage and missing template rule with fake sentinel credentials. Scrub before collection, preserve session paths, fix recursive executable lookup, replace the non-executing guard with a pytest subprocess probe, clarify unsupported write/network guarantees, move hashseed to runner configuration, and remove deprecated Hermes examples.
- **Verification:** Unittest suite executes the documented scaffold in a secret-free temporary child; central CI now runs discovery.
- **Follow-up:** Repository-specific storage/network guards remain required; this scaffold is not an OS sandbox.

## 2026-09-06T02:01:12Z — Close fresh central-template review findings (Codex)

- **Agent/tool:** Codex
- **Repos:** .github
- **Done:** Added real pytest import-time isolation and a child sentinel probe; synchronized the primary test commands; constrained review-agent API permissions to two exact GET instruction fetches; corrected system-log metadata; promoted per-review capture identity with authoritative issue listing.
- **Commits/PRs:** PR #22
- **Verification:** 24 tests passed / 1 child-only skip; 24 unittest subtests passed; changed-workflow actionlint and diff checks passed.
- **Follow-up:** Keep PR #22 draft until current-head CI and a fresh Codex pass are green.

## 2026-09-06T02:02:28Z — Align central capture reviewer boundary (Codex)

- **Agent/tool:** Codex
- **Repos:** .github
- **Done:** Restricted the canonical capture template to supported automated reviewers while retaining per-review identity and authoritative issue lookup.
- **Commits/PRs:** PR #22
- **Verification:** 25 tests passed / 1 child-only skip; 24 unittest subtests passed; changed-workflow actionlint and diff checks passed.
- **Follow-up:** Await current-head CI and Codex review.

<<<<<<< HEAD
## 2026-09-06T07:35:00Z — Secret scan reports finding locations (Claude Code)

- **Agent/tool:** Claude Code
- **Repos:** dizhaky/.github
- **Done:** `reusable-secret-scan.yml` now writes a redacted JSON finding record (`--report-format json --report-path gitleaks-report.json`) on all four scan paths, prints `file:line rule=… commit=…` to `$GITHUB_STEP_SUMMARY` and the job log when the scan step fails, and uploads the report as an artifact. Previously a failure emitted only `leaks found: N`, so locating the finding meant bisecting the tree by hand. `--redact`, the scanned ref range, the trigger surface and the `--exit-code 1` pass/fail semantics are unchanged.
- **Validation:** Executed against gitleaks 8.24.3 (the version this workflow installs) in a scratch directory outside the repo tree, using synthetic non-functional test values. Confirmed exit 1 on a leak and exit 0 on a clean diff; `Secret`/`Match` recorded as `REDACTED` with the raw values absent from the report; `File`/`StartLine`/`RuleID`/`Commit` present and correctly capitalised. Summary step exercised for findings, empty-report, and missing-report cases. `python3 -m unittest discover -s tests` passed.
- **Commits/PRs:** PR pending (draft).
- **Follow-up:**
  - The `Working-tree secret scan` job that caused `obsidian-vault#375` is **not** this workflow — it is a locally defined `worktree` job in `dizhaky/obsidian-vault/.github/workflows/secret-scan.yml` running `gitleaks detect --no-git --source .` directly. It has the same missing-`--report-path` blind spot and is unfixed by this change.
  - `reusable-nightly-maintenance.yml` has the same defect in its rolling scan (it has `--verbose` but no report path).
  - A red `Secret Scan` on `main` still alerts nowhere; `obsidian-vault` `main` was red ~4.7h across five commits before a PR tripped over it. Alerting is a separate question from legibility and is deliberately not addressed here.
=======
## 2026-09-06T02:30:00Z — Make post-merge review capture fork-safe (Codex)

- **Agent/tool:** OpenAI Codex
- **Repos:** .github
- **Done:** Split capture into an unprivileged identifier-payload workflow and a trusted workflow-run receiver; added closed-issue no-op replay handling, bounded review bodies, and base-branch metadata.
- **Verification:** 12 offline workflow tests, actionlint structural validation, direct ShellCheck of both scripts, and git diff --check.

## 2026-09-06T07:36:24+00:00 — capture security follow-up (DAN-3344)

- Agent: Codex
- Repos: dizhaky/.github
- Independent audit corrected source-run validation, per-review serialization, full issue pagination and visible failures. Offline capture suite: 17 tests passed. No live fork canary; deployment awaits PR merge.
- Tracks [DAN-3344](https://linear.app/dizhaky/issue/DAN-3344); earlier 12-test result predates this audit.
>>>>>>> f356391 (fix: validate and deduplicate fork-safe review capture [DAN-3344])
