## 2026-09-06T07:35:00Z — Secret scan reports finding locations (Claude Code)

- **Agent/tool:** Claude Code
- **Repos:** dizhaky/.github
- **Done:** `reusable-secret-scan.yml` now writes a redacted JSON finding record (`--report-format json --report-path gitleaks-report.json`) on all four scan paths, prints `file:line rule=… commit=…` to `$GITHUB_STEP_SUMMARY` and the job log when the scan step fails, and uploads the report as an artifact. Previously a failure emitted only `leaks found: N`, so locating the finding meant bisecting the tree by hand. `--redact`, the scanned ref range, the trigger surface and the `--exit-code 1` pass/fail semantics are unchanged.
- **Validation:** Executed against gitleaks 8.24.3 (the version this workflow installs) in a scratch directory outside the repo tree, using synthetic non-functional test values. Confirmed exit 1 on a leak and exit 0 on a clean diff; `Secret`/`Match` recorded as `REDACTED` with the raw values absent from the report; `File`/`StartLine`/`RuleID`/`Commit` present and correctly capitalised. Summary step exercised for findings, empty-report, and missing-report cases. `python3 -m unittest discover -s tests` passed.
- **Commits/PRs:** PR pending (draft).
- **Follow-up:**
  - The `Working-tree secret scan` job that caused `obsidian-vault#375` is **not** this workflow — it is a locally defined `worktree` job in `dizhaky/obsidian-vault/.github/workflows/secret-scan.yml` running `gitleaks detect --no-git --source .` directly. It has the same missing-`--report-path` blind spot and is unfixed by this change.
  - `reusable-nightly-maintenance.yml` has the same defect in its rolling scan (it has `--verbose` but no report path).
  - A red `Secret Scan` on `main` still alerts nowhere; `obsidian-vault` `main` was red ~4.7h across five commits before a PR tripped over it. Alerting is a separate question from legibility and is deliberately not addressed here.
