# System log — 2026-05-22

## 2026-05-22T19:45:00Z — Debug scan: github-ops hardening (completed)

- **Agent/tool:** Cursor
- **Repos:** dizhaky/.github
- **Done:**
  - Fixed `rollout-nightly.py` template path (`parent.parent`), added `--dry-run`, safer stderr decode
  - Fixed `nightly-health-check.yml` GH_PAT check (env var, not `secrets.*` in bash)
  - Tightened `dependabot-auto-merge.yml` to github-actions ecosystem only
  - Added `scripts/debug_scan.py` for local validation (TEMPLATE_DIR, ruff, YAML, workflow guards)
  - Merged via PR #5; added `.gitignore` for scan artifacts
- **QC:** `python3 scripts/debug_scan.py` → PASS (0 errors)
- **Follow-up:** Use `rollout-nightly.py --dry-run` before live rollouts
