name: Secret Scan

on:
  push:
  pull_request:

permissions:
  contents: read
  pull-requests: read

jobs:
  scan:
    permissions:
      contents: read
      pull-requests: read
    uses: dizhaky/.github/.github/workflows/reusable-secret-scan.yml@main
