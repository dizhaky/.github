name: Secret Scan

on:
  push:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  scan:
    permissions:
      contents: read
      pull-requests: read
    uses: ./.github/workflows/reusable-secret-scan.yml
